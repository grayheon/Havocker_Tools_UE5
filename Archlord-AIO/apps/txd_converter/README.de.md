# 🎨 TXD Converter

Wandelt `.txd`-Dateien in `.dds` und `.png` um – je nach enthaltenem Format.

## Funktionen
- Erkennt DXT1, DXT3, DXT5 → schreibt `.dds`
- Konvertiert DDS nach PNG mit texconv
- Erkennt Raw-Grafiken → erstellt direkt PNG
- Legt nur benötigte Zielordner an (PNG/DDS)

