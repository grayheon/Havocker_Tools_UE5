# 🗺️ Minimap Generator

Creates a composed world map from available tile images.

## Features
- Scans for valid `mapXXXXa/b/c/d.*` tile files
- Combines 2×2 tiles (64 small tiles = 1 full map segment)
- Merges all into a complete worldmap image
- Exports result as PNG or BMP
